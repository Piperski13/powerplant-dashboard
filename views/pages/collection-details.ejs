<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- account, menu icons -->
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&icon_names=account_circle,menu"
    />

    <link rel="icon" href="/assets/icons/favicon.png" type="image/svg+xml" />

    <!-- Local Styles -->
    <link rel="stylesheet" href="/styles/global.css" />
    <link rel="stylesheet" href="/styles/table.css" />

    <title>Collection details</title>
  </head>

  <body>
    <div class="app-shell">
      <aside class="app-sidebar">
        <%- include("../partials/sidebar.ejs") %>
      </aside>

      <div class="app-main">
        <main class="app-content records-view-page">
          <nav class="breadcrumbs">
            <a href="/workspaces/"> Workspaces </a>

            <span>/</span>

            <a href="/workspaces/<%= workspace.id %>">
              <%= workspace.name %>
            </a>

            <span>/</span>

            <span> <%= collection.name %> </span>
          </nav>
          <section class="entity-details">
            <h2><%= collection.name %></h2>

            <p class="entity-details__description">
              <%= collection.description %>
            </p>

            <dl class="entity-details__meta">
              <dt>Created</dt>
              <dd><%= collection.created_at %></dd>

              <dt>Updated</dt>
              <dd><%= collection.updated_at %></dd>
            </dl>
          </section>
          <div class="div-tables-section">
            <div id="div-filter-form">
              <form
                action="/workspaces/<%= workspace.id %>/collections/<%= collection.id %>"
                method="GET"
                id="filter-form"
              >
                <label class="filter-label" for="title"
                  >Filter data by title</label
                >
                <input
                  id="title"
                  name="title"
                  type="text"
                  placeholder="Search by Title"
                  value="<%= title || '' %>"
                />
                <div class="filter-submit">
                  <button
                    type="submit"
                    id="submit-filter"
                    class="form-submit-filter"
                  >
                    Filter
                  </button>
                </div>
              </form>
            </div>

            <div class="entity-actions">
              <form
                action="/workspaces/<%= workspace.id %>/collections/<%= collection.id %>/records/new"
                method="GET"
              >
                <button type="submit" class="button button--primary">
                  + New Record
                </button>
              </form>
            </div>

            <table id="big-table">
              <caption>
                List of Records
              </caption>
              <thead>
                <tr>
                  <th>ID</th>
                  <th>Title</th>
                  <th>Description</th>
                  <th>Documents</th>

                  <% if (user.id === workspace.owner_id) { %>
                  <th>Update</th>
                  <th>Delete</th>
                  <% } %>
                </tr>
              </thead>
              <tbody>
                <% if (records && records.length > 0) { %> <%
                records.forEach(record => { %>
                <tr
                  class="clickable-row"
                  data-href="/workspaces/<%= workspace.id %>/collections/<%= collection.id %>/records/<%= record.id %>"
                >
                  <td data-label="ID"><%= record.id %></td>
                  <td data-label="Title"><%= record.title %></td>

                  <td data-label="Description"><%= record.description %></td>

                  <td data-label="Documents">
                    <div class="documents-list">
                      <% if (record.files && record.files.length > 0) { %> <%
                      record.files.forEach(file => { %>
                      <a
                        class="file-link"
                        href="<%= file.downloadUrl %>"
                        download
                      >
                        <%= file.original_name.slice(0, 10) + '...' %>
                      </a>
                      <br />
                      <br />
                      <% }) %> <% } else { %>
                      <em>No files</em>
                      <% } %>
                    </div>
                  </td>

                  <% if (user.id === workspace.owner_id) { %>
                  <td data-label="Update">
                    <form
                      action="/workspaces/<%= workspace.id %>/collections/<%= collection.id %>/records/<%= record.id %>/edit"
                      method="GET"
                      style="display: inline"
                    >
                      <button type="submit" class="update-record">
                        Update
                      </button>
                    </form>
                  </td>

                  <td data-label="Delete">
                    <form
                      action="/workspaces/<%= workspace.id %>/collections/<%= collection.id %>/records/<%= record.id %>/delete"
                      method="POST"
                      style="display: inline"
                    >
                      <button type="submit" class="delete-record">
                        Delete
                      </button>
                    </form>
                  </td>
                  <% } %>
                </tr>
                <% }) %> <% } else { %>
                <tr>
                  <td colspan="5">No collections found.</td>
                </tr>
                <% } %>
              </tbody>
            </table>
          </div>
          <div class="back-navigation">
            <a
              href="/workspaces/<%= workspace.id %>"
              class="button button--secondary"
            >
              ← Back to Workspace
            </a>
          </div>
        </main>
      </div>
    </div>
    <script src="/scripts/table-row-navigation.js"></script>
  </body>
</html>
